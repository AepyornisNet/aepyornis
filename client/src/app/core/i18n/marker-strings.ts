import { _ } from '@ngx-translate/core';

// Add backend-provided messages here so they can be extracted into PO files.
// Replace or extend the list below with real backend message strings.
export const BACKEND_MESSAGES = [_('Example backend message')];
